---
title: ListRequest
description: user submits a collection
type: object

# This should be ListRequestJson<T> and data should be T, but jsonschema2pojo doesn't capture the
# generic type when generating code.
javaType: com.sift.api.representations.ListRequestJson

properties:
  data:
    type: array
    items:
      type: object
      javaType: java.lang.Object
additionalProperties: false
